#include <stdio.h>
#include <math.h>
#include <limits.h>

int main() {
    // Input
    long long int a, b, n;
    scanf("%lld %lld %lld", &a, &b, &n);

    // Simulate game
    for (int i = 0; i < n; i++) {
        a = 
    }

    // Mod just in case
    a = a % (1000000007);
    b = b % (1000000007);

    // Output
    printf("%d %d", a, b);
}